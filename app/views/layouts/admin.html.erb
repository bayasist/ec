<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      html, body {
        margin: 0;
      }
      header {
        border-bottom: solid 1px gray;
        display: flex;
        position: relative;
      }
      .header--right {
        right: 0;
        position: absolute;
      }
    </style>
  </head>
  <body>
    <header class="header">
      <%= link_to admin_path do %>
        管理画面
      <% end %>
      <nav class="header--right">
        <% if @current_admin_user.present? %>
          <%= @current_admin_user.user_name %>
          <%= link_to "ログアウト", sign_out_admin_admin_users_path %>
        <% else %>
          <%= link_to "ログイン", sign_in_admin_admin_users_path %>
        <% end %>
      </nav>
    </header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
